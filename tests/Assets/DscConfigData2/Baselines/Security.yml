Configurations:
  - SecurityBase
  - WindowsFeatures

WindowsFeatures:
  Name:
    - -Telnet-Client

SecurityBase:
  Role: Baseline

DscTagging:
  Layers:
    - '[x={ Get-DatumSourceFile -Path $File } =]'
